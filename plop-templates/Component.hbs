import type { FC, ComponentPropsWithoutRef } from 'react';
import { cn } from '~lib/utils/tailwind';
{{#if addProps}}

export type {{pascalCase name}}Props = {
  // TODO: Add props or remove this type
};{{/if}}
{{#if addProps}}

type FCProps = FC<Omit<ComponentPropsWithoutRef<'{{componentType}}'>, keyof {{pascalCase name}}Props> & {{pascalCase name}}Props>

{{/if}}
export const {{pascalCase name}}: {{#if addProps}}FCProps{{else}}FC<ComponentPropsWithoutRef<'{{componentType}}'>>{{/if}} = ({ className, ...props }) => {
  return (
    <{{componentType}} className={cn(className)} {...props}>
      {{pascalCase name}}
    </{{componentType}}>
  );
};

{{pascalCase name}}.displayName = '{{pascalCase name}}';
