import React from 'react';
import { observer } from 'mobx-react';

const RegisterInNetwork = () => {
  return (
    <>
      Register in network component
    </>
  );
};

export default observer(RegisterInNetwork);
