import styled from 'styled-components';

export const OperatorName = styled.div`
  font-size: 12px;
  margin-left: 0;
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
`;

export const OperatorScore = styled.div`
  font-size: 10px;
  margin-left: 0;
  font-weight: bold;
`;

export const OperatorKey = styled.div`
  font-size: 10px;
  margin-left: 0;
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
  max-width: 100%;
`;
