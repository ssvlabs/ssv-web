import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    paper: {
        height: '100%',
        width: '100%',
    },
    header: {
        textAlign: 'center',
    },
});
