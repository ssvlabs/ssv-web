export enum UPDATE_FEE_STEPS {
    // eslint-disable-next-line no-unused-vars
    DECLARE_FEE = 1,
    // eslint-disable-next-line no-unused-vars
    WAITING = 2,
    // eslint-disable-next-line no-unused-vars
    PENDING = 3,
    // eslint-disable-next-line no-unused-vars
    EXECUTE = 4,
}
